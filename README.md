This is the core tokenizer for the Dom-X programming language. It is the first stage of the Dom-X compiler pipeline, responsible for breaking down source code into logical tokens.
The engine is built using C-style pointer logic to ensure high-speed character scanning. It serves as the foundation for the native logic used in the Dom-02 operating system.

!Lead Developer --- ardis ---!
